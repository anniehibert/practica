#include <stdio.h>
//standard IO library, lets you read input from the keyboard (standard in) and output to the screen (standard out)

int main() //this declares the main function of the program

{ //marks a block of code
	printf("This is output from my first program!\n"); //el n funciona como el puts
	return 0;
}